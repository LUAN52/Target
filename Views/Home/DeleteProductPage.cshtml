@model Product


<div class="formCont">
  <h1>Excluir Produto</h1>
  <form asp-action="DeleteProduct" asp-controller="Home" asp-route-id="@Model.Id" class="form">
    <div class="form-group">
      <label for="productName">Nome produto</label>
      <input type="text" class="form-control" placeholder="digite seu email" name="productName" value="@Model.Name"
        disabled>
    </div>
    <div class="form-group">
      <label for="exampleInputPassword1">Password</label>
      <input type="text" class="form-control" placeholder="preço" name="price" value="@Model.Price" disabled>
    </div>
    <div class="buttonContainer">
      <div>
        <button type="submit" class="btn btn-primary">Apagar Produto</button>
        <a asp-action ="Index" asp-controller="Home"id="cancelarId"  class="btn btn-danger">Cancelar</a>
      </div>
    </div>
  </form>
</div>