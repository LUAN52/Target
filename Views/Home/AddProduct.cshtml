@model Product

@{
    if (User.Identity.IsAuthenticated)
    {
        <div class="formCont">
            <div class="titleContaier">
                <h1 class="subTtitle"> Cadastrar Produto</h1>
            </div>
            <form asp-action="AddProduct" asp-controller="Home" class="form">
                <div class="form-group">
                    <label for="name">Nome produto</label>
                    <input id="nameId" type="text" class="form-control" placeholder="digite o produto" name="name">
                    <span id="nameErrorId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label for="category">Categoria</label>
                    <input id="categoryId" type="text" class="form-control" placeholder="digite a categoria" name="category">
                    <span id="categoryErrorId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label>Preço</label>
                    <input type="text" class="form-control" id="precoId" placeholder="digite o preço" name="price">
                    <span id="priceErrorId" class="text-danger"></span>
                </div>
                <div class="buttonContainer">
                    <div>
                        <button id="aqui" type="submit" class="btn btn-primary">Adicionar Produto</button>
                        <button  asp-action ="Index" asp-controller="Home"id="cancelarId"  class="btn btn-danger">Cancelar</button>
                    </div>
                </div>
            </form>
        </div>
    }
    else
    {
        <div class="loginContainer">
            <h2>Necessário realizar login</h2>
            <a asp-action="Login" asp-controller="Account" id="logar" type="submit"
        class="btn btn-primary">Logar-se</a>
        </div>

    }
}

<script src="~/js/site.js"></script>